Azure App Service SSL certificates have a **resource-group dependency**. When moving the App Service resources to another resource group, the certificate needs to be handled separately.

The SSL cert needs to be deleted 