#### Azure Public IP
- **IP Version** → IPv4 or IPv6.
- **SKU** → Must match the **Load Balancer SKU**.
- **Tier** → Must match the **Load Balancer tier**.
- **Static IP** → Assigned when created; remains until the public IP resource is deleted.

**Exam Fact:** Public IP **SKU + Tier must match** the Load Balancer.