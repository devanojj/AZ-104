1.

What must be ensured when creating subnets within a virtual network to avoid IP address conflicts?

 

Each subnet must have a public IP address.

All subnets must use the same CIDR block.

The range for one subnet can't overlap with other subnet IP address ranges in the same virtual network.

Correct

2.

Which type of IP address should be used for an Azure resource that needs to connect with Azure public-facing services?

 

Private IP address

Public IP address

Correct

Static IP address

3.

When deploying a database server in Azure that should only be accessible by internal applications, which type of IP address should be assigned?

 

Private IP address

Correct

Dynamic IP address

Public IP address

4.

In which scenario would a dynamic IP address be preferable over a static IP address for Azure resources?

 

Hosting a website with SSL certificates

Running short-term virtual machines for testing purposes

Correct

Setting up an IP address-based security model

5.

During the creation of a virtual network in Azure, what must be defined to ensure the network can be correctly configured?

 

DNS server settings

IP address space

Network Security Groups

Incorrect

6.

An Azure virtual machine has lost connection to the internet after being restarted. What could be a reason for this issue related to IP addressing?

 

The virtual machine is using an IPv6 address

The virtual machine uses a dynamic public IP address

Correct

The virtual machine is assigned a static private IP address

7.

Your company wants to use Azure Virtual Network to create logical divisions within their network for better security and performance. Which feature should you use to implement this segmentation?

 

Subnets

Correct

Public IP addresses

VPN Gateways

8.

Why is it important to statically assign IP addresses to domain controllers in an Azure virtual network?

 

To improve internet connectivity speeds

To maximize dynamic IP address availability

To ensure consistent DNS resolution and avoid frequent IP updates

Correct

9.

While designing a virtual network in Azure, you need to improve security and manageability by dividing it into smaller segments. What is the most appropriate Azure component to achieve this?

 

Application Gateways

Load Balancers

Subnets

Correct