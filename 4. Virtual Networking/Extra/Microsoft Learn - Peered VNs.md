Default VN peered with up to 500 networks 
[[Azure Virtual Network Manager]] make it 1000 networks 
You can peer networks in the same or different regions 
Traffic VM's on Azure Infrastructure 

The network latency between virtual machines in peered virtual networks in the same region is the same as the latency within a single virtual network

 To confirm that virtual networks are peered, you can check effective routes. Check routes for a network interface in any subnet in a virtual network.

You can also troubleshoot connectivity to a virtual machine in a peered virtual network by using [[Azure Network Watcher]]. 

Connectivity check - traffic from virtual machine's network interface to virtual machine's network interface.

